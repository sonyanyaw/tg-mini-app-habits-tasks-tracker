# Habit & Task Tracker (Telegram Mini App)

Простое мини-приложение Telegram для отслеживания ежедневных привычек и задач. Создано с использованием FastAPI (Python) в серверной части и React + Vite (TypeScript) во внешнем интерфейсе. 

## Особенности

*   **Аутентификация через Telegram:** Безопасный вход с использованием данных пользователя Telegram.
*   **Задачи (Tasks):** Создание, выполнение и удаление задач.
*   **Привычки (Habits):** Создание привычек, отслеживание прогресса по дням недели.
*   **Календарь:** Выбор даты для просмотра задач.

## Технологии

### **Фронтенд (React + TypeScript + Vite)**

*   **React 18** с хуками.
*   **TypeScript** для строгой типизации.
*   **Vite** для быстрой сборки.
*   **Redux Toolkit** для управления состоянием.
*   **Axios** для HTTP-запросов.
*   **@telegram-apps/sdk** для интеграции с Telegram Web App API.

### **Бэкенд (FastAPI + PostgreSQL)**

*   **FastAPI** — современный, быстрый (высокопроизводительный) веб-фреймворк для создания API на Python 3.12+.
*   **SQLAlchemy 2.0** с поддержкой асинхронности.
*   **Alembic** для миграций базы данных.
*   **PostgreSQL** (через Supabase) как основная СУБД.
*   **asyncpg** как асинхронный драйвер для PostgreSQL.
*   **Pydantic** для валидации данных.
*   **Aiogram 3.x** (опционально, если используется бот для уведомлений).

## 📁 Структура проекта (Monorepo)
  ```text
    habit-task-tracker/
    ├── backend/                 # Папка с бэкендом (Python/FastAPI)
    │   ├── app/                # Основной модуль приложения
    │   │   ├── init.py
    │   │   ├── main.py         # Точка входа FastAPI
    │   │   ├── database.py     # Подключение к БД
    │   │   ├── models/         # Модели SQLAlchemy
    │   │   ├── schemas/        # Pydantic-схемы
    │   │   ├── api/            # Роутеры API
    │   │   │   ├── init.py
    │   │   │   ├── auth.py
    │   │   │   ├── tasks.py
    │   │   │   └── habits.py
    │   │   └── utils/          # Вспомогательные функции
    │   │       ├── init.py
    │   │       └── telegram_auth.py # Проверка подписи Telegram initData
    │   ├── alembic/            # Миграции Alembic
    │   ├── .env                # Переменные окружения (не коммитить!)
    │   ├── requirements.txt    # Зависимости Python
    │   └── ...
    ├── frontend/               # Папка с фронтендом (React/Vite)
    │   ├── public/             # Статические файлы
    │   ├── src/                # Исходный код React
    │   │   ├── components/     # Переиспользуемые компоненты
    │   │   ├── pages/          # Страницы приложения
    │   │   ├── hooks/          # Кастомные хуки
    │   │   ├── store/          # Redux store и слайсы
    │   │   ├── services/       # API-сервисы (axios)
    │   │   └── ...
    │   ├── .env                # Переменные окружения фронтенда
    │   ├── vite.config.ts      # Конфигурация Vite
    │   └── ...
    ├── .gitignore              # Игнорируемые файлы
    └── README.md               # Этот файл
```

## Начало работы (локально)

### **1. Клонирование репозитория**

```bash
git clone https://github.com/sonyanyaw/tg-mini-app-habits-tasks-tracker.git
cd habit-task-tracker
```
### **2. Настройка бэкенда**

1. Перейдите в папку backend:
```bash
cd backend
```
2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate     # Windows
```
3. Установите зависимости:
```bash
pip install -r requirements.txt
```
4. Создайте файл '.env' и укажите переменные окружения:
```env
DATABASE_URL=postgresql+asyncpg://postgres:ваш_пароль@ваш_хост:5432/ваша_бд
TELEGRAM_BOT_TOKEN=ваш_токен_бота
```
5. Примените миграции Alembic:
```bash
alembic upgrade head
```
6. Запустите сервер:
```bash
uvicorn app.main:app --reload
```
Сервер будет доступен по адресу http://localhost:8000.

### **3. Настройка фронтенда**

1. Откройте новый терминал.

2. Перейдите в папку frontend:
```bash
cd frontend
```
3. Установите зависимости:
```bash
npm install
```
4. Создайте файл '.env' и укажите переменные окружения:
```env
VITE_API_BASE_URL=http://localhost:8000
```
5. Запустите сервер разработки:
```bash
npm run dev
```
Фронтенд будет доступен по адресу http://localhost:5173.

## Деплой 
### Бэкенд (на Amvera) 

1. Подготовьте код бэкенда.

2. Укажите переменные окружения в настройках Amvera:
    - DATABASE_URL
    - TELEGRAM_BOT_TOKEN
    - PYTHONPATH=/app
    - Задеплойте приложение.
     
### Фронтенд (на Vercel) 

1. Подключите Vercel к вашему GitHub-репозиторию.

2. Укажите Root Directory как frontend.

3. Указать в Output Directory 'build' (Project Settings -> Build and Development -> Framework Settings -> Output Directory)

4. Укажите переменные окружения в настройках Vercel:
    - VITE_API_BASE_URL=https://ваш-бэкенд.amvera.io
         
5. Vercel автоматически соберёт и задеплоит приложение.
     

## Настройка Telegram Bot 

1. Создайте бота через @BotFather .
2. Используйте команду /newapp, чтобы cоздать мини-приложение.
3. (Опционально) Добавьте кнопку Web App в меню бота через /setmenubutton.
     
